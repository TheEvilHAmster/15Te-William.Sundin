import java.util.Random;
import java.util.Scanner;
import javax.swing.JFrame;

public class RandomNr extends JFrame{

private static final long serialVersionUDI = 1L;
	
	public static void main(String[] args){
		new RandomNr().setVisible(true);
	}

	private RandomNr(){
		super("ljanflanflanflnafe");
		setSize(600, 600);
		setResizable(true);
		setDefaultCloseOperation(EXIT_ON_CLOSE);
		setLocation(500,250); 
		
	}
	
}
	/*public static void main(String[] args) {
		
			try(Scanner scanner = new Scanner(System.in)){
		
		Random rand = new Random();	
		int numG = rand.nextInt(100);
		int numT = 0;
		int G;
		boolean win = false;
		
		
		while (win == false){
	
		System.out.println("Guess a nr between 1 and 100:");
		G = scanner.nextInt();
		numT++;
		
		if (G == numG){
			win = true;
		}
		else if (G + 30 <= numG || G - 30 >= numG) {
			System.out.println("It's ice cold!!");
		}
		else if (G + 20 <= numG || G - 20 >= numG) {
			System.out.println("It's a little warm");
		}
		
		else if (G + 5 < numG || G - 5 > numG) {
			System.out.println("It's hot!");
		}
		
		else if (G < numG || G > numG) {
			System.out.println("You are Burning it is so HOT!!");
		}
		

	}
	System.out.println("You are right!");
	System.out.println("The nr was " + numG);
	System.out.println("It took you " + numT + " tries");
		}
  }		

}*/